= Dimensional Analysis

This Model has a huge amount of paramaters to control, which need to be fine tuned.



#table(
  columns: 2,
)[*Parameter*][*Dimension*][
  bold(S)][$E/L$
][
  bold(B)][$L E$
][
  bold(Q)][
]

